# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real secrets.
#
# The JWT secrets below are pre-filled with usable dev values so local
# development works out of the box. They must NOT be reused in production —
# generate unique per-environment secrets there instead:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

NODE_ENV=development
HOST=0.0.0.0
PORT=3001

DB_PATH=database.sqlite

JWT_ACCESS_SECRET=lAGiYqJGI6I9U2Im6X01DNzl431yZNNuYpR6KlF62ok=
JWT_REFRESH_SECRET=es9jDURhKj2mHRnUjibYYMMj9Likr6zxH4yBP37m1kA=
